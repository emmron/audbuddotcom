---
const currentPath = Astro.url.pathname;
---

<header>
	<div class="header-content">
		<a href="/" class="logo-link" aria-label="Medicinal Cannabis Reviews - Home">
			<h1 class="site-title">Medicinal Cannabis Reviews</h1>
		</a>
		<nav aria-label="Main navigation">
			<div class="nav-links" id="nav-links">
				<a href="/strains" class={currentPath === '/strains' ? 'active' : ''} aria-current={currentPath === '/strains' ? 'page' : undefined}>
					<span class="icon" aria-hidden="true">🌿</span>
					<span>Strains</span>
				</a>
				<a href="/community" class={currentPath === '/community' ? 'active' : ''} aria-current={currentPath === '/community' ? 'page' : undefined}>
					<span class="icon" aria-hidden="true">👥</span>
					<span>Community</span>
				</a>
				<a href="https://cannabisreviews.com/blog" rel="noopener">
					<span class="icon" aria-hidden="true">📝</span>
					<span>Blog</span>
				</a>
			</div>
			<div class="auth-buttons" role="navigation" aria-label="User authentication">
				<a href="/signin" class="auth-button">Sign In</a>
				<a href="/signup" class="auth-button highlight">Sign Up</a>
			</div>
			<button 
				class="mobile-menu-button" 
				aria-label="Toggle menu" 
				aria-expanded="false" 
				aria-controls="nav-links"
				aria-haspopup="true"
			>
				<span class="bar"></span>
				<span class="bar"></span>
				<span class="bar"></span>
			</button>
		</nav>
	</div>
</header>

<style>
	/* Core Layout */
	header {
		background: #fff;
		padding: 1rem;
		position: sticky;
		top: 0;
		z-index: 1000;
		box-shadow: 0 2px 8px rgba(0,0,0,0.08);
	}

	.header-content {
		max-width: 1200px;
		margin: 0 auto;
		display: flex;
		justify-content: space-between;
		align-items: center;
		height: 3.5rem;
	}

	/* Logo */
	.logo-link {
		text-decoration: none;
		padding: 0.75rem;
		border-radius: 8px;
		transition: background-color 0.2s ease;
	}

	.logo-link:hover {
		background: rgba(76, 175, 80, 0.08);
	}

	.site-title {
		margin: 0;
		font-size: 1.5rem;
		font-weight: 700;
		color: #111827;
		letter-spacing: -0.02em;
	}

	/* Navigation */
	nav {
		display: flex;
		align-items: center;
		gap: 2rem;
	}

	.nav-links {
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	nav a {
		color: #374151;
		text-decoration: none;
		font-size: 1rem;
		font-weight: 500;
		padding: 0.75rem 1rem;
		border-radius: 8px;
		transition: all 0.2s ease;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	nav a:not(.auth-button):hover {
		color: #111827;
		background: rgba(17, 24, 39, 0.05);
	}

	.icon {
		font-size: 1.25rem;
		opacity: 0.8;
		transition: opacity 0.2s ease;
	}

	nav a:hover .icon {
		opacity: 1;
	}

	.active {
		color: #111827 !important;
		background: rgba(17, 24, 39, 0.05);
		font-weight: 600;
	}

	/* Auth Section */
	.auth-buttons {
		display: flex;
		gap: 0.75rem;
		margin-left: 2rem;
		padding-left: 2rem;
		border-left: 1px solid #e5e7eb;
	}

	.auth-button {
		padding: 0.75rem 1.25rem;
		border-radius: 8px;
		font-weight: 500;
		font-size: 1rem;
		transition: all 0.2s ease;
	}

	.auth-button:not(.highlight) {
		color: #374151;
	}

	.auth-button:not(.highlight):hover {
		color: #111827;
		background: rgba(17, 24, 39, 0.05);
	}

	.auth-button.highlight {
		background: #4f46e5;
		color: white;
	}

	.auth-button.highlight:hover {
		background: #4338ca;
		transform: translateY(-1px);
	}

	/* Mobile Menu */
	.mobile-menu-button {
		display: none;
		flex-direction: column;
		gap: 6px;
		padding: 0.75rem;
		background: transparent;
		border: none;
		cursor: pointer;
		border-radius: 8px;
		transition: background-color 0.2s ease;
	}

	.mobile-menu-button:hover {
		background: rgba(17, 24, 39, 0.05);
	}

	.bar {
		width: 24px;
		height: 2px;
		background: #111827;
		transition: transform 0.2s ease;
		border-radius: 2px;
	}

	/* Responsive */
	@media (max-width: 1200px) {
		.header-content {
			padding: 0 2rem;
		}
	}

	@media (max-width: 768px) {
		.nav-links,
		.auth-buttons {
			display: none;
			position: absolute;
			top: 100%;
			left: 0;
			right: 0;
			background: #fff;
			padding: 1rem;
			flex-direction: column;
			gap: 0.5rem;
			border-bottom: 1px solid #e5e7eb;
			box-shadow: 0 4px 16px rgba(0,0,0,0.1);
		}

		.nav-links.show,
		.auth-buttons.show {
			display: flex;
		}

		.auth-buttons {
			border: none;
			margin: 0;
			padding: 1rem;
			background: #f9fafb;
		}

		.mobile-menu-button {
			display: flex;
		}

		nav a,
		.auth-button {
			width: 100%;
			justify-content: center;
			padding: 1rem;
		}

		.auth-button.highlight {
			margin-top: 0.5rem;
		}
	}

	@media (max-width: 480px) {
		.header-content {
			height: 3.5rem;
			padding: 0 1rem;
		}
		
		.site-title {
			font-size: 1.25rem;
		}
	}

	@media (prefers-reduced-motion: reduce) {
		* {
			transition: none !important;
		}
	}
</style>
</style>
<script>
	// Type definitions for better type safety
	type MenuElements = {
		button: HTMLButtonElement | null;
		navLinks: HTMLElement | null;
		authButtons: HTMLElement | null;
		bars: HTMLElement[]; // Fixed type from NodeListOf
	};

	type AnimationConfig = {
		readonly open: readonly [string, string, string];
		readonly closed: readonly [string, string, string];
	};

	// Core animation and transition constants
	const ANIMATION = {
		DURATION: 300,
		TIMING: 'cubic-bezier(0.4, 0, 0.2, 1)',
		TRANSFORMS: {
			open: [
				'rotate(45deg) translate(6px, 6px)',
				'scale(0)',
				'rotate(-45deg) translate(6px, -6px)'
			],
			closed: ['none', 'none', 'none']
		} as AnimationConfig
	} as const;

	// Initialize DOM elements with error handling
	function initializeElements(): MenuElements {
		const button = document.querySelector('.mobile-menu-button') as HTMLButtonElement | null;
		const navLinks = document.querySelector('.nav-links') as HTMLElement | null;
		const authButtons = document.querySelector('.auth-buttons') as HTMLElement | null;
		// Convert NodeList to Array and ensure HTMLElement type
		const bars = Array.from(document.querySelectorAll('.bar')).map(el => {
			if (!(el instanceof HTMLElement)) {
				throw new Error('Bar element is not HTMLElement');
			}
			return el;
		});

		if (!button || !navLinks || !authButtons || bars.length !== 3) {
			throw new Error('Failed to initialize required menu elements');
		}

		return { button, navLinks, authButtons, bars };
	}

	// Enhanced hamburger animation with error boundaries
	function animateHamburger(elements: MenuElements, isOpen: boolean): void {
		try {
			elements.bars.forEach((bar, index) => {
				const transition = `transform ${ANIMATION.DURATION}ms ${ANIMATION.TIMING}, opacity ${ANIMATION.DURATION}ms ${ANIMATION.TIMING}`;
				const transform = ANIMATION.TRANSFORMS[isOpen ? 'open' : 'closed'][index];
				const opacity = isOpen && index === 1 ? '0' : '1';

				requestAnimationFrame(() => {
					bar.style.transition = transition;
					bar.style.transform = transform;
					bar.style.opacity = opacity;
				});
			});
		} catch (error) {
			console.error('Error in hamburger animation:', error instanceof Error ? error.message : 'Unknown error');
		}
	}

	// Accessibility announcer utility with ARIA live region
	function announceToScreenReader(message: string): void {
		try {
			const ariaLive = document.createElement('div');
			ariaLive.setAttribute('aria-live', 'polite');
			ariaLive.setAttribute('aria-atomic', 'true');
			ariaLive.className = 'sr-only';
			ariaLive.textContent = message;
			
			document.body.appendChild(ariaLive);
			setTimeout(() => {
				if (document.body.contains(ariaLive)) {
					document.body.removeChild(ariaLive);
				}
			}, ANIMATION.DURATION + 700);
		} catch (error) {
			console.error('Screen reader announcement failed:', error instanceof Error ? error.message : 'Unknown error');
		}
	}

	// Core menu state handler with error boundaries and performance optimization
	function handleMenuState(elements: MenuElements, isOpen: boolean): void {
		try {
			const { button, navLinks, authButtons } = elements;
			if (!button || !navLinks || !authButtons) return;

			// Update ARIA attributes for accessibility
			button.setAttribute('aria-expanded', isOpen.toString());
			button.setAttribute('aria-label', `${isOpen ? 'Close' : 'Open'} menu`);

			// Batch DOM updates using requestAnimationFrame
			requestAnimationFrame(() => {
				// Apply transitions
				[navLinks, authButtons].forEach(element => {
					element.style.transition = `opacity ${ANIMATION.DURATION}ms ${ANIMATION.TIMING}, transform ${ANIMATION.DURATION}ms ${ANIMATION.TIMING}`;
					element.classList.toggle('show', isOpen);
				});

				// Handle animations and body states
				animateHamburger(elements, isOpen);
				document.body.style.overflow = isOpen ? 'hidden' : '';
				document.body.classList.toggle('menu-open', isOpen);
			});

			announceToScreenReader(isOpen ? 'Menu opened' : 'Menu closed');

		} catch (error) {
			console.error('Error handling menu state:', error instanceof Error ? error.message : 'Unknown error');
			// Restore safe state
			document.body.style.overflow = '';
			document.body.classList.remove('menu-open');
		}
	}

	// Initialize and set up event listeners with error handling and debouncing
	function initializeMenu(): void {
		try {
			const elements = initializeElements();
			let isProcessing = false;
			let lastClickTime = 0;
			const DEBOUNCE_THRESHOLD = 200; // ms

			// Click handler with debounce
			elements.button.addEventListener('click', (e: MouseEvent) => {
				const now = Date.now();
				if (isProcessing || (now - lastClickTime) < DEBOUNCE_THRESHOLD) return;

				isProcessing = true;
				lastClickTime = now;
				e.stopPropagation();

				const isExpanded = elements.button?.getAttribute('aria-expanded') === 'true';
				handleMenuState(elements, !isExpanded);

				setTimeout(() => { isProcessing = false; }, DEBOUNCE_THRESHOLD);
			});

			// Outside click handler with validation
			document.addEventListener('click', (e: MouseEvent) => {
				const target = e.target as Node;
				const isMenuOpen = elements.navLinks?.classList.contains('show');
				const isClickOutside = !elements.button.contains(target) && 
					!elements.navLinks.contains(target) &&
					!elements.authButtons.contains(target);

				if (isMenuOpen && isClickOutside) {
					handleMenuState(elements, false);
				}
			});

			// Keyboard navigation
			document.addEventListener('keydown', (e: KeyboardEvent) => {
				if (e.key === 'Escape' && elements.navLinks?.classList.contains('show')) {
					handleMenuState(elements, false);
				}
			});

			// Cleanup handler
			window.addEventListener('beforeunload', () => {
				document.body.style.overflow = '';
				document.body.classList.remove('menu-open');
			});

		} catch (error) {
			console.error('Menu initialization failed:', error instanceof Error ? error.message : 'Unknown error');
		}
	}

	// Initialize the menu system when DOM is ready
	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', initializeMenu);
	} else {
		initializeMenu();
	}
</script>
</script>